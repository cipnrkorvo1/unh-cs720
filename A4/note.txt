// A4 test driver
void *globalPtr = 0;
long anotherPtr = 0x23456789;

int main()
{
    int status;
    void *ptr[10];
    status = memInitialize(100);

    globalPtr = memAllocate(15, NULL);
    printf("memAllocate returned %p\n", globalPtr);

    for (int i = 0; i < 5; i++)
    {
        ptr[i] = memAllocate(15, NULL);
        printf("memAllocate[%d] returned %p\n", i, ptr[i]);
    }

    memDump();
}